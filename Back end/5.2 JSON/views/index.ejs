<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <% if(!locals.selectedRecipe){ %>
      <!-- If no recipe yet, show: -->
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } else { %>
      <!-- Else, show the recipe information -->
      <% let ingredients = selectedRecipe.ingredients; %>
      <% let protein = ingredients.protein; %>
      <% let salsa = ingredients.salsa; %>
      <% let toppings = ingredients.toppings; %>

      <h2 id="recipeTitle">
        <%- selectedRecipe.name %>
      </h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%- protein.name %>, <%- protein.preparation %></li>
        <li><%- salsa.name %></li>
        <% toppings.forEach(topping => { %>
        <li><%- topping.quantity %> <%- topping.name %></li>
        <% }); %>
      </ul>
    <% } %>

  </div>
</body>

</html>